<div class="container p-3">
  <div class="row">
    <%= render "public/shared/error_messages", resource: resource %>
    <div class="col">

      <h3 class="mt-3">初めてご利用の方</h3>
        <p>こちらから会員登録を行ってください。</p>

      <%= form_with model: @user, url: user_registration_path do |f| %>
      <div class="w-100 my-4 py-4">


        <table class="table table-borderless mb-1">
          <tr>
            <td class="text-right">アカウント名</td>
            <td><span class="text-muted necessary-field p-1 px-2 ml-2 small rounded">必須</span></td>
            <td><%= f.text_field :account_name, autofocus: true, placeholder:"アカウント名", class: "form-control" %></td>
          </tr>
          <tr>
            <td class="text-right">メールアドレス</td>
            <td><span class="text-muted necessary-field p-1 px-2 ml-2 small rounded">必須</span></td>
            <td><%= f.text_field :email, placeholder: "sample@example.com", class: "w-100 form-control" %></td>
          </tr>
          <tr>
            <td class="text-right">パスワード<% if @minimum_password_length %></td>
            <td><span class="text-muted necessary-field p-1 px-2 ml-2 small rounded">必須</span>
              <br><span class="small">(<%= @minimum_password_length %> 文字以上)</span>
            </td>
            <% end %>
            <td><%= f.password_field :password, class: "form-control" %></td>
          </tr>
          <tr>
            <td class="text-right">パスワード</td>
            <td><span class="text-muted necessary-field p-1 px-2 ml-2 small rounded">必須</span>
              <br><span class="small">(確認用)</span>
            </td>
            <td><%= f.password_field :password_confirmation, class: "form-control" %></td>
          </tr>
        </table>
      </div>
      <div class="text-center mb-3">
        <%= f.submit "新規登録", class:"btn btn-dark px-3 mt-4" %>
      </div>
      <% end %>

      <div class="col-12">
        <h4 class="mt-5">既に登録済みのお客様</h4>
      <div>
        <%= link_to "こちら", new_user_session_path, class: "text-muted rounded-pill necessary-field px-4 mb-5", 'data-turbolinks': false %>からログインを行ってください。
      </div>
    </div>
  </div>
</div>

