投稿編集

<h1>投稿内容入力</h1>
<%= form_with model: @lifehack do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <h4>画像</h4>
  <%= f.file_field :lifehack_image, accept: "image/*" %>
  <h4>おすすめ度</h4>
  <div class="form-group">
      <%= render "static.rate", lifehack: @lifehack %>
  </div>

  <%= f.submit '変更を保存', class: "btn btn-dark px-4" %>
<% end %>
<%= link_to '詳細ページ', @lifehack %> |
<%= link_to '投稿一覧', lifehacks_path, class: "back" %>